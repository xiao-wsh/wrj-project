package com.md.basePlatform.mapper.impl; // Mapper 实现包占位（说明类所在位置，实际 SQL 在 resources）

/**
 * 数据操作层实现包说明（作业结构说明用）
 * <p>
 * 本项目采用 MyBatis XML 作为 Mapper 实现：<br>
 * - 接口：com.md.basePlatform.mapper.api.DroneMapper <br>
 * - 实现：resources/mapper/impl/DroneMapper.xml <br>
 * 因此这里不需要再写 Java 实现类。
 * </p>
 *
 * <p>这个类只是占位文档用途，不会产生任何实例。</p>
 */
public final class MapperImplPackageNote { // final 修饰：仅作文档占位，不允许被继承

    /**
     * 私有构造器：阻止外部通过 new 创建本类对象
     */
    private MapperImplPackageNote() { // 私有构造器，任何人包括子类都无法实例化
        // 工具说明类，不允许实例化——方法体有意保持为空
    }
}
